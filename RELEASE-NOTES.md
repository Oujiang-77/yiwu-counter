# 0.1.3

- 商品资料可单个或批量删除；删除前有二次确认，删除后同步移出当前待开单清单，历史报价单保持不变。
- 商品资料编辑字段改为每行三项，可拖动或点击箭头调整顺序；顺序保存在本机，重新打开和查看商品详情时保持一致。
- 商品资料新增颜色、外箱尺寸、单箱重量、材质和产品参数，支持编辑、Excel 导入模板与字段匹配，以及报价单字段选择和导出。
- 提供 Windows 安装程序。每次安装均更新当前用户固定目录中的程序，并更新同名桌面快捷方式，避免解压多个版本后仍点开旧程序。
- 程序发现同一数据目录内仍运行着其他版本时，明确提示先退出旧版，不再直接打开旧页面。
- 商品资料、图片、备份和导出记录继续保存在独立数据目录。旧便携版目录不会自动删除。

升级：下载并运行 `HuoYouShu-Setup-0.1.3-Windows-x64.exe`。先退出正在运行的旧版；首次安装后使用安装程序创建的桌面图标。后续安装程序仍安装到同一目录。GitHub 下载受限时可转发安装程序文件。

# 0.1.2

- 报货单导出字段统一为“单价”和“总金额”；字段配置与 Excel 表头同步更新。
- 导出字段内部键同步调整为 `unitPrice` 和 `totalAmount`，旧配置值会在读取时自动归一化。

升级：在程序内点击“检查更新”，确认后会自动下载、校验、替换程序并重启。商品资料、图片和数据目录不会被更新包覆盖。

# 0.1.1

- Excel 支持拖拽导入，上传上限从 30 MB 提高至 500 MB；每批仍最多 10000 行、100 列。
- 字段对应页面分为数据读取、字段匹配、信息补充三块；读取选项同行，未选字段留空，必填项红星标记。
- 说明销售编码补充规则：缺少编码时使用前缀和原表行序号生成，例如 A-01；已有编码保留，重复编码会提示。
- 分块保存大文件、按行读取数据、逐张处理图片，重复预览复用解析结果。
- 加入 GitHub 版本检查和自动更新助手；点击“检查更新”后可校验、替换程序并自动重启，不再需要手动解压后续版本。
- 保留本机商品资料、图片和已选择的数据目录。
- 商品资料列表保留主图列，并在操作列前增加彩盒列；两列分别展示首图并标注各自图片数量。
- 商品新增和 Excel 导入仅要求销售编码、厂家编码、商品名称；厂家名称、装箱数和出厂价可留空。
- 移除批量查询示例、列表辅助说明和厂家为空时的补充提示。

验证：9 项原有业务测试、导入缓存及限制测试、浏览器拖拽/布局/校验/写入测试；272 MiB 构造文件完成本地上传与图片预览。未取得用户朋友的原始 271 MB 文件，仍需实文件复测。单张图片限制 15 MB，旧版 .xls 图片需手动补充。

升级：本次基础包需要手动交付一次。之后在程序内点击“检查更新”，确认后会自动完成下载、校验、替换和重启。不要手动删除或覆盖商品数据目录。
