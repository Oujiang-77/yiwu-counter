# 档口开单系统设计规范

> 用途：后续在新会话继续开发前，先阅读本文档。本文档记录当前真实进度、数据规则、发布方式和待办事项。

## 1. 项目概况

- 项目目录：`C:\Users\gy2026032701\Documents\个人\yiwu-counter-app`
- GitHub 仓库：`https://github.com/Oujiang-77/yiwu-counter`
- 当前程序版本：`0.1.1`（本地待发布）
- 当前分支：`main`
- 运行方式：Windows 本地程序 + 本机浏览器页面 + SQLite
- 数据原则：商品资料、图片、草稿、备份和导出记录均保存在本机，不依赖云数据库。
- 当前状态：已生成并交付一份本地版压缩包；用户朋友目前使用的是尚未接入在线更新检查的旧包。

## 2. 当前已经完成的功能

- 商品资料库：销售编码、厂家编码、商品名称、厂家、包装、价格、起订量等字段。
- 商品图片：点击或拖拽上传 JPG / PNG / WEBP；有图片后右上角 `×` 删除。
- Excel 导入：字段映射、预览校验、问题行处理、图片导入支持。
- 报货单导出：按厂家生成 Excel，支持合并文件或分厂家 ZIP。
- 图片开单：本机 OCR 识别，识别后人工核对商品、数量和单位。
- 厂家信息、导出记录、备份恢复、退出系统等本地功能。
- 界面调整：顶部占位区域隐藏；导出记录放入左侧菜单；品牌区域和提示文案已按确认稿调整。
- 数据存储位置：商品资料页显示当前目录，旁边有编辑图标。
- 应用内文件夹浏览器：支持桌面、文档、下载、磁盘入口；可逐级进入、返回上一级、新建文件夹、选择当前文件夹。
- 首次启动：没有历史数据时自动打开文件夹选择器；用户选择后记忆目录。

## 3. 数据存储和升级安全规则

### 默认目录

新用户首次启动前，默认临时使用：

`%LOCALAPPDATA%\HuoYouShu`

用户选择其他目录后，会在默认目录下写入：

`%LOCALAPPDATA%\HuoYouShu\data-location.json`

该文件只记录当前数据目录路径。

### 切换目录

- 新目录必须是空文件夹。
- 系统会复制数据库、商品图片、备份和导出记录。
- 原目录不会自动删除。
- 选择新目录后，重启程序会按 `data-location.json` 读取新目录。
- 如果用户取消首次选择，仍可先使用默认目录，之后通过商品资料页的编辑图标再次选择。

### 版本更新时

更新程序文件时不能覆盖用户数据目录，也不能删除：

- `counter.sqlite3`
- `images/`
- `backups/`
- `exports/`
- `data-location.json`

更新助手只替换程序目录，并在替换前等待旧程序退出；数据目录保持原样。更新完成后自动启动新版本并在页面提示升级成功。

## 4. GitHub 更新准备进度

已经完成本地代码准备，但尚未向 GitHub 推送，也尚未创建 Release：

- `update.py`：固定读取 `Oujiang-77/yiwu-counter` 的 GitHub Releases，解析版本、更新说明、ZIP 下载地址和 SHA256 文件。
- `app.py`：新增 `GET /api/update/check`，浏览器只请求本机接口，不直接跨域请求 GitHub。
- `frontend-live.js`：检查更新弹窗读取真实更新源；发现新版本后可启动自动更新，不会假装更新成功。
- `package_release.py`：除 ZIP 和 SHA256 文件外，还会生成版本更新清单。
- `.github/workflows/release.yml`：推送 `v0.1.1` 这类标签后，GitHub Actions 会在 Windows runner 上构建并创建 Release。

### 当前限制

本次基础包已经包含自动更新助手。助手会下载 ZIP、校验 SHA256、等待旧程序退出、替换程序目录并启动新版本；失败时会保留旧程序。朋友手上的旧包不包含在线更新检查代码，因此仍需先手动交付本次基础包；从本次基础包开始，后续版本可在程序内更新。

## 5. 后续版本迭代标准流程

### 开发阶段

1. 在新会话开头先阅读本文件，再确认本次要改的功能和是否需要更新版本号。
2. 修改源码；不要把用户数据、`review-ui/`、测试数据库或照片放进发布包。
3. 如果是界面功能，修改源文件后执行 `node build_frontend.cjs` 生成 `web/app.js` / `web/app.css`。
4. 运行语法检查、业务测试和 `git diff --check`。

### 发布阶段

1. 修改 `storage.py` 中的 `VERSION`，例如从 `0.1.0` 改为 `0.1.1`。
2. 更新使用说明或 Release notes，写清新增功能和注意事项。
3. 执行：

   ```powershell
   node build_frontend.cjs
   .\.venv\Scripts\python.exe -m py_compile app.py update.py
   .\.venv\Scripts\python.exe test_local.py
   .\build.ps1
   ```

4. 检查上一级 `outputs/` 目录中的 ZIP、`.sha256.txt` 和更新清单。
5. 提交并推送源码到 GitHub。
6. 创建并推送与程序版本一致的标签，例如：

   ```powershell
   git tag v0.1.1
   git push origin main --tags
   ```

7. GitHub Actions 自动构建 Windows 包并创建 Release。
8. 发布后用朋友电脑或干净临时目录验证：启动、检查更新、下载、数据目录和商品图片是否保留。

## 6. 下一项明确待办

### 商品资料操作栏增加“修改”

- 当前操作栏已有商品详情入口，编辑表单已经存在。
- 后端已经有：`PUT /api/products/{pid}`。
- 下一步只需在商品表格的操作栏增加“修改”按钮，直接调用现有编辑表单，保存后刷新列表。
- 该功能适合作为下一次版本迭代，例如 `0.1.1`，同时把更新检查基础能力打入新包。

### 自动更新助手

- 主程序把自身复制到临时目录后启动独立 helper，避免运行中的 EXE 被覆盖。
- helper 下载 ZIP 并校验 SHA256。
- helper 等待旧程序退出后替换程序目录，再启动新版本。
- 用户选择的数据目录、数据库、商品图片、备份和导出记录不在替换范围内。
- 新版本通过 `--updated` 打开页面并提示更新完成；失败时保留旧程序。

## 7. 常用验证命令

```powershell
# 仅运行业务测试
.\.venv\Scripts\python.exe test_local.py

# 重新生成生产前端
node build_frontend.cjs

# 检查源码语法
.\.venv\Scripts\python.exe -m py_compile app.py update.py

# 本地运行评审服务（不要把 review-ui/ 打进发布包）
.\.venv\Scripts\python.exe app.py --data-dir review-ui --port 18765 --no-browser
```

## 8. 发布前注意事项

- 不要把 `review-ui/`、`test-*`、SQLite 数据库、用户照片和本地日志打进发布包。
- 不要覆盖或删除用户选择的数据目录。
- GitHub 仓库存在不等于已经有可用 Release；必须创建版本标签并上传 Release 资产。
- 私有仓库不能把访问令牌硬编码进客户端；当前方案按公开仓库 Releases 设计。
- GitHub 在国内可能访问较慢，正式使用前应准备国内对象存储或备用下载地址。
- 每次发布必须保证标签版本、`storage.VERSION`、ZIP 文件名三者一致。


## 9. 2026-09-21 导入功能更新（本地已完成，未发布）

- 用户最新要求：先看本地改动，暂停远程发布；GitHub 仓库暂不继续排查。
- 上传区支持单个 Excel 文件拖拽与点击选择，统一校验格式、500 MB 上限；读取中禁止重复上传。
- 字段对应分为数据读取、字段匹配、信息补充三块。工作表、表头行和重新读取表头同行。
- 字段未选择时显示空白；销售编码、厂家编码、商品名称标红星。厂家名称、装箱数和出厂价可留空。
- 商品资料列表保留主图列，并在操作列前增加彩盒列；两列分别展示首图并标注各自图片数量。
- 销售编码为空且填写前缀时，按前缀加原表数据行序号生成；前缀 A、起始 1 生成 A-01、A-02 等。原表已有编码不覆盖；混合已有编码时，自动编码可能不连续。重复编码仍报错。
- 上传以 1 MB 分块写入文件；xlsx 只读按行解析，嵌入图片逐张处理；同一工作表及表头的重复预览复用解析缓存。
- 文件上限 500 MB；展开总量上限 2 GB，非媒体数据展开上限 200 MB；10000 行、100 列、单张图片 15 MB 的限制保留。
- 修复清空全部字段映射后仍自动猜测映射的问题。
- 已验证：原有 9 项业务测试、2 项新回归、浏览器拖拽和完整导入、约 285 MB（272 MiB）构造文件上传及图片预览。未取得朋友的原始文件，因此不能承诺其复杂图片公式或文件结构完全兼容。
- 后续发布使用 RELEASE-NOTES.md；版本检查支持当前打包脚本产生的 SHA256 文件名。用户数据、review-ui 和测试资料不得入包。
